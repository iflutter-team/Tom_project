import 'package:flutter/material.dart';

class editeditprofile extends StatelessWidget {
  const editeditprofile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child:  Text("edit_profile_screen",style: TextStyle(fontSize: 25),),
      ),
    );
  }
}
